name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: pcov
          tools: composer:v2

      - name: Get composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

      - name: Cache composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Start SSH agent for OpenFinancy dependencies
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.OPENFINANCY_SSH_PRIVATE_KEY }}

      - name: Add GitHub to known_hosts
        run: ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Force Composer VCS clones to use SSH
        run: |
          git config --global url."git@github.com:OpenFinancy/".insteadOf "https://github.com/OpenFinancy/"
          git config --global url."git@github.com:openfinancy/".insteadOf "https://github.com/openfinancy/"

      - name: Install dependencies
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: PHPMD
        run: vendor/bin/phpmd src text rulesets/phpmd.xml --exclude tests

      - name: Static analysis
        run: composer analyse

      - name: Coding standards
        run: composer lint

      - name: Run tests
        run: vendor/bin/phpunit --coverage-text

